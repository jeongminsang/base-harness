{
  "version": "0.1",
  "facts": [
    { "k": "preset",  "v": "{{PRESET}}" },
    { "k": "build",   "v": "{{BUILD_CMD}}" },
    { "k": "lint",    "v": "{{LINT_CMD}}" },
    { "k": "srcDir",  "v": "{{SRC_DIR}}" }
  ],
  "skills": []
}
